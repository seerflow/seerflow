# S-180-F3: Backfill the EPIC-DOC retrospective per-story actual-points data

**Parent:** S-180 (SEE-195) — Seerflow Guide v1.1 capstone
**Linear:** _(not yet created — Linear workspace at free-issue limit at the time of S-180 closure; create when capacity returns)_
**Related issues:** SEE-195, SEE-142
**Priority:** Low
**Story Points:** 1
**Status:** Draft

---

## Why

The staged retrospective at `documents/seerflow-guide-staging/guide/retros/epic-doc-2026-04.md` (published via S-180-F1) has placeholder `TBD` cells in the per-story actual-points + cycle-time table because the originating S-180 session did not run live Linear MCP queries against every child issue. This story fills those cells in.

## Scope

1. For each of the 13 EPIC-DOC child stories (SEE-174 through SEE-181, SEE-190, SEE-192, SEE-193, plus S-175 and S-176 — confirm their Linear IDs if any), call `mcp__plugin_linear_linear__get_issue` and extract `createdAt`, `startedAt`, `completedAt`, and any estimate updates between create and complete.
2. Compute calendar days from `startedAt` → `completedAt`, planned-vs-actual point delta (using whatever final `estimate` ended up on the issue minus the original story-file points).
3. Replace the `TBD` cells in the per-story table.
4. Optional: interview the writer(s) for a "what was harder than estimated" colour-commentary row and append it to the relevant entries.

## Acceptance Criteria

- [ ] Every row in the retro's per-story table has populated Actual and Δ cells.
- [ ] Underlying data backed up as a CSV alongside the markdown for future analyses.
- [ ] Updated retrospective re-posted (as an edit) to the Linear comment on SEE-142.

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] Retro CSV committed alongside the markdown.

## Linear sync

When Linear capacity returns, create a new SEE-XXX issue with this content and update the `**Linear:**` header above.
