import React from "react";
import { ScreenStub } from "./ScreenStub";

/**
 * Alerts list screen — P0b stub.
 * Renders a "coming soon" panel until the Alerts redesign (S-320).
 * S-320 will replace the body of this file.
 */
export const AlertsScreen: React.FC = () => {
  return <ScreenStub label="Alerts" />;
};
