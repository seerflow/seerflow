import React from "react";
import { ScreenStub } from "./ScreenStub";

/**
 * Alert detail screen — P0b stub.
 * Renders a "coming soon" panel until the Alert detail redesign (S-320).
 * S-320 will replace the body of this file.
 */
export const AlertDetailScreen: React.FC = () => {
  return <ScreenStub label="Alert detail" />;
};
