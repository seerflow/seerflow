# Seerflow dashboard frontend environment variables

# Base URL of the Seerflow API (leave blank for same-origin deployment).
VITE_API_BASE=

# Comma-separated list of origins permitted for the WebSocket connection when
# VITE_API_BASE resolves to a non-same-origin host. Empty = same-origin only.
VITE_WS_ORIGIN_ALLOWLIST=
