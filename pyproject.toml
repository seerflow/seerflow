[project]
name = "seerflow"
version = "0.1.0"
description = "A streaming, entity-centric log intelligence agent that foresees operational failures and sees security threats hiding across your log sources"
requires-python = ">=3.13"
dependencies = []
